#!/bin/bash
#can only be accesss within the function
#create using the "local" keyword
# - local LOCAL_VAR=1
#Only functions can have local variables
#Best practice to keep variables local in functions

